{
  "name": "roommate",
  "private": true,
  "type": "module",
  "workspaces": [
    "roommate-app",
    "roommate-server"
  ],
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^6.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.7.0",
    "typescript-eslint": "^8.62.0"
  },
  "scripts": {
    "dev": "docker compose down && docker compose up",
    "dev:build": "docker compose down && docker compose up --build",
    "dev:app": "npm run dev --workspace=roommate-app",
    "dev:server": "npm run dev --workspace=roommate-server",
    "db:seed": "npm run db:seed --workspace=roommate-server",
    "db:studio": "npm run db:studio --workspace=roommate-server",
    "lint": "eslint .",
    "lint:app": "eslint roommate-app",
    "lint:server": "eslint roommate-server",
    "lint:fix": "eslint . --fix",
    "format:check": "npm run format:check --workspace=roommate-app",
    "type-check:app": "npm run type-check --workspace=roommate-app",
    "type-check:server": "npm run type-check --workspace=roommate-server",
    "type-check": "npm run type-check:app && npm run type-check:server",
    "build:app": "npm run build --workspace=roommate-app",
    "test": "npm test --workspace=roommate-server"
  }
}