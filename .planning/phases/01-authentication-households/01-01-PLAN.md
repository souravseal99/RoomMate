---
phase: 01-authentication-households
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/household-members/householdMember.repo.ts
  - src/household-members/householdMember.service.ts
  - src/household-members/householdMember.controller.ts
  - src/household-members/householdMember.routes.ts
autonomous: true
requirements:
  - HOUSE-04
user_setup: []

must_haves:
  truths:
    - "User can leave a household they belong to"
    - "Leaving removes the membership record but preserves household data"
    - "Admin leaving transfers admin role to another member or allows household deletion"
  artifacts:
    - path: "src/household-members/householdMember.repo.ts"
      provides: "Database operations for leaving household"
      exports: ["leave"]
    - path: "src/household-members/householdMember.service.ts"
      provides: "Business logic for leave household"
      exports: ["leave"]
    - path: "src/household-members/householdMember.controller.ts"
      provides: "HTTP endpoint for leave"
      exports: ["leave"]
    - path: "src/household-members/householdMember.routes.ts"
      provides: "Route registration"
      exports: ["POST /leave/:householdId"]
  key_links:
    - from: "src/household-members/householdMember.service.ts"
      to: "src/household-members/householdMember.repo.ts"
      via: "HouseholdMemberRepo.leave()"
      pattern: "HouseholdMemberRepo\\.leave"
    - from: "src/household-members/householdMember.controller.ts"
      to: "src/household-members/householdMember.service.ts"
      via: "HouseholdMemberService.leave()"
      pattern: "HouseholdMemberService\\.leave"
---

<objective>
Implement leave household functionality - the missing piece to complete HOUSE-04 requirement.

Purpose: Allow users to voluntarily exit a household while preserving household data for remaining members.

Output: Working leave household API endpoint that handles member removal, admin role transfer, and edge cases.
</objective>

<context>
@roommate-server/src/household-members/householdMember.repo.ts
@roommate-server/src/household-members/householdMember.service.ts
@roommate-server/src/household-members/householdMember.controller.ts
@roommate-server/src/household-members/householdMember.routes.ts
@roommate-server/prisma/schema.prisma

Reference existing household service patterns:
- join() handles duplicate membership check
- household.service.ts shows proper error handling pattern
</context>

<tasks>

<task type="auto">
  <name>Add leave method to HouseholdMemberRepo</name>
  <files>roommate-server/src/household-members/householdMember.repo.ts</files>
  <action>
    Add static async leave(userId: string, householdId: string) method to HouseholdMemberRepo class.
    - Use prisma.householdMember.delete with where: { userId, householdId }
    - Handle Prisma error P2025 (record not found) gracefully - return null if membership doesn't exist
    - Return the deleted record or null
  </action>
  <verify>grep -n "static async leave" roommate-server/src/household-members/householdMember.repo.ts</verify>
  <done>HouseholdMemberRepo.leave() exists and returns deleted record or null</done>
</task>

<task type="auto">
  <name>Add leave method to HouseholdMemberService</name>
  <files>roommate-server/src/household-members/householdMember.service.ts</files>
  <action>
    Add static async leave(userId: string, householdId: string) method:
    - Validate user exists in household using HouseholdMemberRepo.isExistingUser()
    - If not a member, return ApiResponse.error("Not a member of this household", StatusCodes.NOT_FOUND)
    - If user is ADMIN role, check if other members exist:
      - If no other members, delete the entire household using HouseholdRepository.delete()
      - If other members exist, transfer admin to the next member (first one found)
    - Call HouseholdMemberRepo.leave(userId, householdId)
    - Return success response with message "Left household successfully"
    
    Reference: household.service.ts join() method for error handling patterns
  </action>
  <verify>grep -n "static async leave" roommate-server/src/household-members/householdMember.service.ts</verify>
  <done>HouseholdMemberService.leave() exists with admin transfer logic</done>
</task>

<task type="auto">
  <name>Add leave controller and route</name>
  <files>
    roommate-server/src/household-members/householdMember.controller.ts
    roommate-server/src/household-members/householdMember.routes.ts
  </files>
  <action>
    **controller.ts**: Add static async leave(request: Request, response: Response) method:
    - Extract householdId from request.params
    - Extract userId from request.body.user (set by ensureAuthenticated middleware)
    - Call HouseholdMemberService.leave(userId, householdId)
    - Return response with status and data

    **routes.ts**: Add new route:
    - POST /leave/:householdId with ensureAuthenticated middleware
    - Route: householdRouter.post("/leave/:householdId", ensureAuthenticated, HouseholdMemberController.leave)
  </action>
  <verify>grep -n "leave" roommate-server/src/household-members/householdMember.routes.ts</verify>
  <done>POST /household-member/leave/:householdId endpoint registered</done>
</task>

</tasks>

<verification>
Backend leave household endpoint works:
- curl -X POST http://localhost:3000/household-member/leave/{householdId} -H "Authorization: Bearer {token}" returns 200
- User membership is removed from database
- If admin was sole member, household is deleted
</verification>

<success_criteria>
- [x] User can leave a household (HOUSE-04)
- [x] Leave endpoint responds with appropriate success/error
- [x] Admin transfer logic works when admin leaves with other members present
- [x] Household deleted when admin leaves as sole member
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-households/01-01-SUMMARY.md`
</output>
