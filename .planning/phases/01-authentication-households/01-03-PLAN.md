---
phase: 01-authentication-households
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - roommate-ui/src/api/household.ts
  - roommate-ui/src/pages/Dashboard.tsx
  - roommate-ui/src/pages/CreateHousehold.tsx
  - roommate-ui/src/pages/JoinHousehold.tsx
  - roommate-ui/src/pages/HouseholdMembers.tsx
  - roommate-ui/src/components/HouseholdCard.tsx
  - roommate-ui/src/App.tsx
autonomous: true
requirements:
  - HOUSE-01
  - HOUSE-02
  - HOUSE-03
  - HOUSE-04
user_setup: []

must_haves:
  truths:
    - "User can create a new household"
    - "User can join an existing household via invite code"
    - "User can view household members"
    - "User can leave a household"
  artifacts:
    - path: "roommate-ui/src/api/household.ts"
      provides: "API client for household operations"
      exports: ["createHousehold", "joinHousehold", "getHouseholds", "getHouseholdMembers", "leaveHousehold"]
    - path: "roommate-ui/src/pages/Dashboard.tsx"
      provides: "Main household dashboard showing user's households"
    - path: "roommate-ui/src/pages/CreateHousehold.tsx"
      provides: "Form to create new household"
    - path: "roommate-ui/src/pages/JoinHousehold.tsx"
      provides: "Form to join via invite code"
    - path: "roommate-ui/src/pages/HouseholdMembers.tsx"
      provides: "View members of a household"
    - path: "roommate-ui/src/components/HouseholdCard.tsx"
      provides: "Card component for displaying household info"
  key_links:
    - from: "roommate-ui/src/pages/Dashboard.tsx"
      to: "roommate-ui/src/api/household.ts"
      via: "getHouseholds API call"
      pattern: "getHouseholds"
    - from: "roommate-ui/src/pages/CreateHousehold.tsx"
      to: "roommate-ui/src/api/household.ts"
      via: "createHousehold API call"
      pattern: "createHousehold"
---

<objective>
Implement all household management UI on the frontend.

Purpose: Complete the household feature set with create, join, view members, and leave functionality.

Output: Working household dashboard with full CRUD operations via UI.
</objective>

<context>
@roommate-server/src/households/household.routes.ts
@roommate-server/src/households/household.service.ts
@roommate-server/src/household-members/householdMember.routes.ts
@roommate-server/src/household-members/householdMember.service.ts
@roommate-server/src/__tests__/households/household.service.test.ts

Backend API details:
- POST /household/create: { name }, returns { household }
- POST /household/join/:inviteCode: returns { household }
- GET /household/all: returns { household[] }
- POST /household/delete: { householdId }
- GET /household-member/all/:householdId: returns members with user info
- POST /household-member/leave/:householdId: returns success/error
</context>

<tasks>

<task type="auto">
  <name>Create household API client</name>
  <files>roommate-ui/src/api/household.ts</files>
  <action>
    Create axios-based API client with session ID handling:
    
    - createHousehold(name): POST /household/create
    - joinHousehold(inviteCode): POST /household/join/{inviteCode}
    - getHouseholds(): GET /household/all
    - deleteHousehold(householdId): POST /household/delete
    - getHouseholdMembers(householdId): GET /household-member/all/{householdId}
    - leaveHousehold(householdId): POST /household-member/leave/{householdId}
    
    All requests include x-session-id header from localStorage
    All responses return { data, message } structure from ApiResponse
  </action>
  <verify>grep -n "createHousehold\|leaveHousehold" roommate-ui/src/api/household.ts</verify>
  <done>household.ts exports all required functions</done>
</task>

<task type="auto">
  <name>Create Dashboard page</name>
  <files>roommate-ui/src/pages/Dashboard.tsx</files>
  <action>
    Create main dashboard:
    - Fetch user's households on mount using getHouseholds()
    - Display household list using HouseholdCard components
    - Empty state: Show "Create your first household" or "Join a household" prompts
    - Actions: "Create Household" button, "Join Household" button
    - Each card shows: household name, member count, invite code (for sharing)
    
    Behavior:
    - Loading state while fetching
    - Error handling with retry option
    - Click card to navigate to /household/:id/members
  </action>
  <verify>grep -n "getHouseholds" roommate-ui/src/pages/Dashboard.tsx</verify>
  <done>Dashboard loads and displays user's households</done>
</task>

<task type="auto">
  <name>Create CreateHousehold page</name>
  <files>roommate-ui/src/pages/CreateHousehold.tsx</files>
  <action>
    Create household creation form:
    - Name input (type="text", required, minLength=1)
    - Submit button "Create Household"
    - Cancel button to go back
    
    On submit:
    - Call createHousehold(name)
    - On success: redirect to /dashboard with success message
    - On error: display error message
    
    Note: User automatically becomes ADMIN of created household
  </action>
  <verify>grep -n "createHousehold" roommate-ui/src/pages/CreateHousehold.tsx</verify>
  <done>CreateHousehold form works and creates household</done>
</task>

<task type="auto">
  <name>Create JoinHousehold page</name>
  <files>roommate-ui/src/pages/JoinHousehold.tsx</files>
  <action>
    Create join household form:
    - Invite code input (type="text", required)
    - Helper text: "Enter the 8-character code shared by a household member"
    - Submit button "Join Household"
    - Cancel/back button
    
    On submit:
    - Call joinHousehold(inviteCode)
    - On success: redirect to /dashboard with success message
    - On error: display error (invalid code, already member, etc.)
    
    URL pattern for sharing: /join/{inviteCode}
  </action>
  <verify>grep -n "joinHousehold" roommate-ui/src/pages/JoinHousehold.tsx</verify>
  <done>JoinHousehold form works and joins household</done>
</task>

<task type="auto">
  <name>Create HouseholdMembers page</name>
  <files>roommate-ui/src/pages/HouseholdMembers.tsx</files>
  <action>
    Create members view:
    - Fetch members on mount: getHouseholdMembers(householdId)
    - Display list: Name, email, role (ADMIN/MEMBER)
    - Show current user's role
    - If user is ADMIN: Show "Leave Household" button
    - If user is MEMBER: Show "Leave Household" button
    - Back button to return to dashboard
    
    Leave button:
    - Confirmation dialog: "Are you sure you want to leave?"
    - Call leaveHousehold(householdId)
    - On success: redirect to /dashboard with message
    - If was sole admin: Show warning that household will be deleted
    
    Empty state: "No members found" (shouldn't happen)
  </action>
  <verify>grep -n "leaveHousehold\|getHouseholdMembers" roommate-ui/src/pages/HouseholdMembers.tsx</verify>
  <done>Members page shows list and leave functionality works</done>
</task>

<task type="auto">
  <name>Create HouseholdCard component</name>
  <files>roommate-ui/src/components/HouseholdCard.tsx</files>
  <action>
    Create reusable card component:
    - Props: household { householdId, name, inviteCode? }
    - Display: Name as title
    - Show invite code for sharing (truncate or full)
    - Clickable: Navigates to members page
    - Optional: Member count badge
    
    Style: Card with border, padding, hover effect
  </action>
  <verify>grep -n "HouseholdCard" roommate-ui/src/components/HouseholdCard.tsx</verify>
  <done>HouseholdCard component exists and renders household info</done>
</task>

<task type="auto">
  <name>Add household routes to App</name>
  <files>roommate-ui/src/App.tsx</files>
  <action>
    Add routes:
    - /dashboard: Dashboard page (protected)
    - /household/create: CreateHousehold (protected)
    - /household/join: JoinHousehold (protected)
    - /household/join/:inviteCode: JoinHousehold pre-filled (protected)
    - /household/:id/members: HouseholdMembers (protected)
    
    Update ProtectedRoute to include these paths
  </action>
  <verify>grep -n "/household" roommate-ui/src/App.tsx</verify>
  <done>All household routes are protected and accessible</done>
</task>

</tasks>

<verification>
- Navigate to /dashboard → shows household list or empty state
- Click "Create Household" → form → creates household → returns to dashboard with new household
- Click "Join Household" → form with code → joins → returns to dashboard
- Click household card → members page shows list
- Click "Leave Household" → confirm → leaves → returns to dashboard
- Share invite code link works
</verification>

<success_criteria>
- [x] User can create a new household (HOUSE-01)
- [x] User can join an existing household via invite code (HOUSE-02)
- [x] User can view household members (HOUSE-03)
- [x] User can leave a household (HOUSE-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-households/01-03-SUMMARY.md`
</output>
