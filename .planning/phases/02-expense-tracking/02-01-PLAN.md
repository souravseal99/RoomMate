---
phase: 02-expense-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roommate-server/src/expenses/calculateBalance.ts
  - roommate-server/src/expenses/expense.routes.ts
  - roommate-server/src/expenses/expense.controller.ts
  - roommate-server/src/expenses/expense.service.ts
  - roommate-app/src/components/expenses/BalanceSummary.tsx
  - roommate-app/src/components/expenses/ExpenseViewer.tsx
  - roommate-app/src/api/expenseApi.ts
autonomous: true
requirements:
  - EXPN-04
must_haves:
  truths:
    - "User can see balance summary showing who owes whom"
    - "User can see settlement suggestions (minimum transactions)"
    - "All household members shown including $0 balance"
  artifacts:
    - path: "roommate-app/src/components/expenses/BalanceSummary.tsx"
      provides: "Balance display with settlements"
    - path: "roommate-server/src/expenses/calculateBalance.ts"
      provides: "Settlement algorithm"
      exports: ["calculateSettlements"]
    - path: "roommate-app/src/api/expenseApi.ts"
      provides: "fetchBalances method"
  key_links:
    - from: "BalanceSummary.tsx"
      to: "/api/expense/for/:householdId/balances"
      via: "fetchBalances API call"
    - from: "expense.service.ts"
      to: "calculateBalance.ts"
      via: "calculateSettlements function"
---

<objective>
Balance summary UI with settlement suggestions

Purpose: Display who owes whom with calculated settlement suggestions (minimum transactions)
Output: BalanceSummary component showing balances + suggested settlements
</objective>

<context>
@.planning/phases/02-expense-tracking/02-CONTEXT.md
@roommate-server/src/expenses/calculateBalance.ts (existing - needs enhancement)
@roommate-app/src/api/expenseApi.ts (existing - needs fetchBalances)
@roommate-app/src/pages/expenses/Expenses.tsx (existing page - needs BalanceSummary)

## User Decisions (locked):
- Settlement suggestion approach (minimum transactions)
- Simple summary display ("Alice owes Bob $20")
- Show all members including $0 balance
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settlement suggestion algorithm</name>
  <files>roommate-server/src/expenses/calculateBalance.ts</files>
  <action>
    Enhance calculateBalance.ts to add calculateSettlements function that:
    - Takes current balances and returns minimum transactions to settle
    - Algorithm: Greedy approach - match biggest creditor with biggest debtor
    - Returns array of { fromUserId, fromName, toUserId, toName, amount } suggestions
    - Keep existing calculateBalance function unchanged for backward compatibility
  </action>
  <verify>npm test -- --grep "calculateBalance" in roommate-server</verify>
  <done>calculateSettlements returns minimal transactions to settle all debts</done>
</task>

<task type="auto">
  <name>Task 2: Add getBalances endpoint with settlements</name>
  <files>
    roommate-server/src/expenses/expense.routes.ts
    roommate-server/src/expenses/expense.controller.ts
    roommate-server/src/expenses/expense.service.ts
  </files>
  <action>
    Update getBalances to return both:
    - balances: array of { userId, name, balance } for all members (including $0)
    - settlements: array of { fromUserId, fromName, toUserId, toName, amount }
    
    To include $0 balance members: Query all household members and merge with balance calculations
  </action>
  <verify>GET /expense/for/:householdId/balances returns { balances: [...], settlements: [...] }</verify>
  <done>Endpoint returns all household members with balances and settlement suggestions</done>
</task>

<task type="auto">
  <name>Task 3: Add fetchBalances to frontend API</name>
  <files>roommate-app/src/api/expenseApi.ts</files>
  <action>
    Add fetchBalances method to expenseApi:
    - GET /expense/for/${householdId}/balances
    - Returns { balances: [...], settlements: [...] }
  </action>
  <verify>expenseApi().fetchBalances(householdId) returns typed response</verify>
  <done>Frontend can fetch balance data</done>
</task>

<task type="auto">
  <name>Task 4: Create BalanceSummary component</name>
  <files>roommate-app/src/components/expenses/BalanceSummary.tsx</files>
  <action>
    Create BalanceSummary component:
    - Fetches balances and settlements on mount using expenseApi
    - Displays "Who owes whom" section: "Alice owes Bob $20"
    - Displays "Settlement suggestions" section: list of payments to settle
    - Shows all members including $0 balance
    - Real-time recalculation - use useEffect on expenses change
    - Placement: Above expense list in Expenses page (per Claude's discretion)
  </action>
  <verify>Component renders balances and settlement suggestions correctly</verify>
  <done>User can see balance summary and suggested settlements</done>
</task>

<task type="auto">
  <name>Task 5: Add Settled status indicator to ExpenseViewer</name>
  <files>roommate-app/src/components/expenses/ExpenseViewer.tsx</files>
  <action>
    Update ExpenseViewer to show "Settled" badge:
    - Add "Status" column to table
    - For now, show all as "Pending" (will add "Settled" in Plan 02)
    - Future: Check if all splits for expense are settled via Settlement records
  </action>
  <verify>Expense table shows Status column with Pending/Settled</verify>
  <done>Expense list shows status indicator</done>
</task>

</tasks>

<verification>
- Balance summary displays above expense list
- Settlement suggestions show minimum transactions
- All household members shown (including $0 balance)
- Expense viewer has Status column
</verification>

<success_criteria>
User can view balance summary (EXPN-04): balances displayed, settlement suggestions shown
</success_criteria>

<output>
After completion, create .planning/phases/02-expense-tracking/02-01-SUMMARY.md
</output>
